'use client';

import React from 'react';

export default function JobsPage() {
    return (
        <div>
            <h1>Jobs Page</h1>
            <p>This is a temporary version to test JSX parsing.</p>
        </div>
    );
}